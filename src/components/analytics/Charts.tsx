// src/components/analytics/Charts.tsx
// Recharts wrapper for dynamic import — keeps all chart components in one lazy-loaded chunk
"use client";

export {
  BarChart,
  Bar,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  ResponsiveContainer,
  PieChart,
  Pie,
  Cell,
  AreaChart,
  Area,
  Legend,
  RadialBarChart,
  RadialBar,
} from "recharts";
